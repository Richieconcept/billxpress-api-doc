meta {
  name: 5. Admin Sync Provider Plans
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/v1/admin/services/data/plans/sync
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{authToken}}
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "provider": "smeplug"
  }
}

vars:pre-request {
  authToken: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VySWQiOiI2YTFlOGZlM2NiODVhYzVmYjkxN2E0YjIiLCJyb2xlIjoiYWRtaW4iLCJhdXRoVGllciI6InRpZXJfMiIsImlhdCI6MTc4MjgyNDE5NSwiZXhwIjoxNzgzNDI4OTk1fQ.1cVLyOmQtwzpu6WpbQcGA5JqfpgGqBg8eP2srPnr7x8
}

docs {
  # Synchronize Provider Plans
  
  Fetches the current plan catalogue from SME Plug or SMEAPI and saves it in
  BillXpress. Send `"provider": "smeplug"` or `"provider": "smeapi"`.

  New plans are created with `isEnabled: false` and `ourPrice: null`. A later
  synchronization updates provider/network prices but preserves our price,
  datatype, enabled status, and delivery controls.

  Run separately for both catalogue providers:

  ```json
  { "provider": "smeapi" }
  ```

  ```json
  { "provider": "smeplug" }
  ```

  Synchronize after configuring the provider credentials and whenever the
  provider changes its catalogue. Synchronization does not activate a provider
  for a network; use Data Settings for routing.

  Expected response:

  ```json
  {
    "message": "Data plans synchronized successfully",
    "sync": {
      "provider": "smeapi",
      "received": 119,
      "available": 119,
      "disabledByProvider": 0,
      "syncedAt": "2026-07-02T00:00:00.000Z"
    }
  }
  ```
}
